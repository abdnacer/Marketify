import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Header from '../../components/Header'

const PageNotAccess = () => {
  return (
    <View>
      <Header back />
      <Text>PageNotAccess</Text>
    </View>
  )
}

export default PageNotAccess

const styles = StyleSheet.create({})